## Non-Negotiable Constraints

- Implement real features, real workflows, real methods, and real UI/UX. Do not leave placeholder behavior, stub handlers, or TODO-only paths in user-facing flows.
- Avoid unnecessary complexity and AI slop: minimal correct diffs, no over-abstraction, no duplicate modules, no speculative fallbacks.
- Apply zero-memory-leak practices suitable for an always-on desktop agent: remove IPC/renderer listeners on unmount, clear timers/intervals, null window refs on `closed`, avoid accumulating `webContents` subscriptions; prefer `invoke`/`handle` over ad-hoc listener pairs; profile RSS + heap in production-like builds when investigating leaks.
- Preserve Shell Mono styling, layout, UX patterns, features, and behavior across the entire codebase when fixing bugs or adding features — do not redesign unrelated surfaces. Match surrounding components (tokens in `src/renderer/index.css`, `vx-*` / `chrome*` utilities, `SurfaceShell`, feature-local CSS like `dock-flyout.css`); reuse and extend existing abstractions instead of one-off styles; keep selection, spacing, and interaction patterns consistent within a surface and with adjacent surfaces.
- Never remove existing features or functionalities unless the user explicitly requests removal.
- Every feature and UI surface must live in structured, modular files and folders — extend existing modules before creating new ones.

## Implementation Workflow (before coding)

1. **Analyze first** — read relevant code paths, `AGENTS.md`, `project.md`, and adjacent components; understand architecture (main/renderer/shared IPC, Zustand stores, harness + orchestrator loop, Shell Mono UI).
2. **Search before creating** — grep/glob for overlapping functionality; extend or modify existing files rather than duplicating.
3. **Integrate fully** — wire IPC, stores, settings persistence, tests, and styling tokens; run `pnpm build` and targeted Vitest after changes.
4. **Research when needed** — for provider APIs, Electron lifecycle, security, or performance, use current (2026) docs rather than assumptions.
5. **Ask brief questions** when requirements, behavior, or formatting are ambiguous — grounded in what the codebase already does.

## Learned User Preferences

- Never assume, guess, or speculate — verify, confirm, and validate with codebase evidence, logs, screenshots, root or terminal output before reporting or fixing issues.
- When implementing attached plans: do not edit the plan file; use existing todos (do not recreate); mark items in progress; finish all todos; then re-check the plan for missed items, gaps, duplications, and dead code.
- Before large features or refactors: analyze the full codebase and ask brief, concise clarifying questions first.
- Preserve existing Shell Mono styling, layout, UX patterns, features, and behavior across the entire codebase when fixing bugs or adding features — do not redesign unrelated surfaces. (See **Non-Negotiable Constraints** above.)
- Do not hardcode provider or model capabilities; dynamically discover models, context windows, and thinking/reasoning support via provider APIs.
- When implementing provider or model features, research current (2026) docs and APIs online rather than relying on stale assumptions.
- Model picker UX: group providers into categories, left-align model IDs, and place thinking-effort controls inline next to each model (not a separate dropdown).
- Take the time needed for thorough work; keep features in structured, modular files and folders rather than monolithic files.
- Create git commits only when the user explicitly asks.
- Remove Cursor-branded or Cursor-style UI references from Vyotiq where the user has corrected this.
- Bug and screenshot analysis should be end-to-end (conversation logs, app logs, code paths) with root causes verified.
- Do not revive legacy phased-execution types/UI (`phase` / `phase-gate` / `phase-ledger-entry`, PhaseLogRow/PhaseLedgerRow) or the removed reflective-autonomy subsystem (terminal gates, goal mode, `plan` tool, harness `05-reflection-autonomy.md`).

## Learned Workspace Facts

- Vyotiq is the product; Agent V is a single solo agent with direct tool access — no sub-agent or delegation architecture. Behavior is governed by natural-language harness markdown in `src/main/harness/`; tools live under `src/main/tools/` with exposure in `src/main/tools/policy/`; orchestrator loop in `src/main/orchestrator/loop/`.
- Stack: Electron (main/renderer IPC), React, TypeScript, Vite 8 (electron-vite 6), Zustand; Tailwind CSS v4 CSS-first tokens in `src/renderer/index.css` (`@theme`, no `tailwind.config.js`). Shell Mono: stealth-dark oklch palette, Geist Sans/Mono, chromeless timeline, frameless workbench (left dock + main canvas). Primary dev OS is Windows (PowerShell). `project.md` is authoritative. Runtime data/logs: `%APPDATA%\vyotiq\vyotiq\`.
- AI providers use raw HTTP only (no vendor SDKs); models via `GET /v1/models`. Adaptive account poller (5s active / 60s idle) fetches host-aware billing and pushes `providers:account-updated`; discovery poller pushes `providers:models-updated` with pricing/context/thinking. `recentBillingBlock.ts` caches billing failures per provider+model for 5 minutes (`BILLING_BLOCK_TTL_MS`) so switching models is never blocked by a sibling model's error.
- Timeline UI: `src/renderer/components/timeline/`; state in `useChatStore` / `useTimelineUiStore`. Hidden tools: `finish`, `ask_user`, `todos`, `heartbeat`, and `continue` settle in the log but do not render activity rows (`isTimelineHiddenTool`); `ask_user` uses `ask-user-prompt` rows; answers as compact user bubble; `todos` surfaces live in the composer task tray (nested sub-tasks via optional `parentId`, collapsible tree UI). Edit diffs: per-file `FileChangeCard` + `SnippetDiffBody`; unified `DiffViewer` for review line-pick only.
- Prompt/context caching: layered topology in `buildContextLayers.ts` (static system → workspace → history → runtime → turn; no dedicated few-shot slot); provider hints in `src/main/providers/cacheHints/`; metrics in `TokenUsage`; Settings → Agent behavior → Prompt caching.
- Harness = always-on prefix sections (`00`/`01`/`05`) + on-demand **Agent Skills** (bundled SKILL.md under `src/main/skills/bundled/` including `review-checklist`, `pipeline-recipes`, plus workspace `.vyotiq/skills/`, global `%APPDATA%/vyotiq/vyotiq/skills/`, and Cursor-compat `.cursor|agents/skills/`): the model loads skills itself via the `context` tool (`list`/`load`, per-run dedupe) rather than force-feeding bodies every turn. Composer `/skill-name` host-prefetches the skill body before turn 0; `/create-skill` is a manual-only bundled workflow. Skills catalogue is workspace-aware per run; Settings → Agent behavior → Skills. Prefix harness sections remain editable in Settings → Harness. **Single-agent by design** — no sub-agents; optional Settings → Providers **authoring model** (frontier build) vs **default run model** (execute).
- Workbench shell (`WorkbenchShell`): chat left; editor/terminal/preview open a resizable right pane with `[Terminal | Globe | file tabs…]`. Left dock stays visible; `Mod+W` close tab, `Mod+Alt+ArrowUp/Down` cycle. Terminal detach is renderer-only; PTY persists for agent `bash` (`ensureWorkspacePty` / `terminal:attach`).
- Custom keybindings via `settings.ui.keybindings`; `Mod+S` saves dirty editor; `Mod+J` focuses composer (skipped when settings open). Focus: `escapeLayerStack.ts` priority; AskUser soft Tab trap; workbench tab cycle DOM-focuses terminal/editor/browser. Main wakes window on `ask_user`, settlement, scheduled enqueue, second-instance (`requestUserAttention.ts`). Settings close honors user `closeSettings` binding (Escape-only uses escape stack).
- Composer follow-ups: active run Send → steering lane; Queue → queued lane (before finish). During `ask_user`, Send submits answers; Queue defers follow-ups. Stop aborts run without clearing queue; steering persists (tray **prior run** badge). Shortcuts: `Mod+Shift+Enter` Queue, `Escape` Stop. (`src/main/followUps/`, `src/renderer/components/composer/followUps/`). Scheduled runs enqueue into queued lane when conversation is busy (`src/main/scheduler/`).
- Screen capture & vision: composer camera picker (`CaptureScreenButton`, `useCapturePicker`) — user-initiated; narrow ~360px button-anchored panel (not full shell width); lazy JPEG previews; full fidelity via `getUserMedia` + `capture:ingest-frame`; agent `capture` tool uses `capture:request-frame` bridge; optional title redaction in Settings → Screen capture. Multimodal vision: composer attachments (paste/drop/capture) → `prepareVisionParts` / `runVisionQueue`; provider `inputModalities` from discovery; vision tokens in context budget. Drag-drop must not fall back to OS clipboard on failed drop; paste uses clipboard intentionally. Workspace-relative capture paths preserved via `attachments:ingestPaths` + `resolveAttachmentInWorkspace`.
- HTML reports: Settings → Agent behavior → Reports (`settings.ui.reports`) — auto-open, in-app BrowserWindow, host ask_user gate after large edits, token-costing AI footer; free Quick summary via `reports:generate-run-summary`. Auto-open only for live settlements, not transcript replay.
- Editor LSP: optional stdio bridge for in-app CodeMirror (Pyright + TS LS via Electron-as-Node); Settings → Editor LSP; not wired to agent `search`/`read`. Vector re-index: Settings → Vector memory; embedder changes trigger `reindexAllWorkspacesIfVectorMemoryChanged`. Memory append: `memory:write` mode `append` + Settings → Memory UI. `read`/`edit`/editor preserve BOM/EOL via `decodeDiskText.ts`.
- Legacy phased execution removed: engine, harness `05-phased-execution.md`, and timeline rows purged; `normalizeLegacyTranscript.ts` strips `phase` / `phase-gate` / `phase-ledger-entry` on load. Tool re-run and tool permissions removed entirely. Supply chain: pnpm 11 with `minimumReleaseAge` (7 days), `blockExoticSubdeps`, `allowBuilds` in `pnpm-workspace.yaml`; hoisted `node-linker`; frozen `pnpm install --frozen-lockfile` in CI/release (`docs/supply-chain-security.md`); avoid `pnpm dlx` / `npx` for installs.
- Left dock navigator (`DockNavigator`, `DockWorkspaceFolder`, `DockChatStrip`, `DockFilesPanel`): inline Repositories-style tree (workspaces → nested chats → files); per-workspace Files expand state in `useUiStore.filesExpandedWorkspaces`; placeholder chats display as **Untitled** via `displayChatTitles.ts`; selection uses subtle shared row tint in `dock-flyout.css` (no inset bars); workspace ⋯ actions inline on the folder row; GitHub-bound rows use `FolderGit2` + `owner/repo @ branch` subtitle (inactive rows show branch meta).
- **Open workspace** (`OpenWorkspaceDialog`, `Mod+O`): unified **Local folder | GitHub** dialog — browse/paste paths or connect accounts and open repos. GitHub tab: **All | @user | org** scope pills, **Recent** repos (last 5 per account), incomplete-clone **Retry** banner, org list via `github:orgs:list`. GitHub clones land under `%APPDATA%/vyotiq/vyotiq/repos/<login>/<host>/<owner>/<repo>`; partial `.git` dirs are removed and re-cloned on retry. Multi-account tokens in `github.accounts.encrypted.json` (OAuth Device Flow + PAT); optional GHE host + Settings `ui.githubOAuthClientId` / `VYOTIQ_GITHUB_OAUTH_CLIENT_ID` with collapsible OAuth setup guide (callback URL copy helper) in Settings → Workspace data. Account health: `lastVerifiedAt`, Re-verify, stale/error badges. Git progress (`github:git-progress`) drives keyed clone/fetch toasts, live stderr in the dialog before a workspace row exists, and composer branch chip **Fetching…** / **main ↑N ↓N** sync state (`workspace:git-status` ahead/behind after fetch). GitHub-bound workspaces store `source: 'github'` + `github` binding on `WorkspaceEntry`; branch chip in composer status strip (`ComposerBranchChip`) switches branches via `workspaces:switch-branch` (HTTPS credential helper in main only). Dock header exposes **Open from GitHub** alongside add workspace.
- Empty-chat landing (`ChatLandingSetup`): when workspace + provider are ready, shows a mono git context line (`workspace · branch ↑N ↓N · M changed` via `workspace:git-status` ahead/behind + dirty count) above the centered composer. Agent `<workspace_context>` includes compact VCS line with the same upstream sync suffix.
- Mod+K unified search (`DockSearchPopover`, `useDockUnifiedSearch`): skills (inserts `/alias` into composer), cross-workspace chat titles, indexed user-prompt excerpts (`conversations:search` + append-only `prompt-search-index.json`, backfilled on boot), and workspace files; message hits scroll timeline via `usePendingTimelineScroll`.
- Scheduled runs: Settings → Agent behavior → Scheduled runs (`scheduledRuns` IPC + 30s tick); dock titlebar **Scheduled runs** popover (`DockScheduledRunsPopover`) lists enabled schedules for the active workspace with **Manage…** deep link; busy conversations enqueue via follow-up lane (`source: 'scheduled'`).
