/** Live and settled UI warn when a turn exceeds this wall-clock duration. */
export const LONG_TURN_WARN_MS = 120_000;

/** Stronger warning + tooltip for very long turns. */
export const VERY_LONG_TURN_WARN_MS = 480_000;
