/**
 * Model picker layout breakpoints — keep in sync with @container rules in index.css.
 */

/** Max popover width when fitting to the chat column (48rem). */
export const MODEL_PICKER_FIT_MAX_WIDTH_PX = 768;

/** Below this width the list + side panel stack vertically. */
export const MODEL_PICKER_SPLIT_MIN_PX = 416;
